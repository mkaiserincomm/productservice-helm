apiVersion: logging.banzaicloud.io/v1beta1
kind: Flow
metadata:  
  name: {{ include "productservice.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  globalOutputRefs: []
  localOutputRefs:
  - {{ include "productservice.fullname" . }}
  match:
  - select:
      container_names: []
      hosts: []
      labels:
        app.kubernetes.io/name: {{ include "productservice.fullname" . }}